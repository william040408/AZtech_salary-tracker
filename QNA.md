# 개념 질문 기록

이 프로젝트를 만들면서 나온 질문과 답을 주제별로 모았다.
나중에 개념서 형태의 PDF 로 다시 엮을 때 쓴다.

각 항목은 `Q` 질문 / `A` 짧은 답 / `→` 더 파고들 거리 로 되어 있다.
답은 요약이므로, 문서로 만들 때는 그림과 예시를 붙여 늘려야 한다.

---

## 1부 · 웹의 바닥

### Q1. HTML 이 뭐야?
**A.** 화면의 뼈대를 적은 글자 파일. `<h1>제목</h1>` 처럼 "여기는 큰 제목"을 표시한다.
브라우저가 그걸 읽고 화면을 그린다. 레고 조립 설명서와 같다.
→ 태그, 요소, 속성의 개념

### Q2. CSS 는 뭐야?
**A.** 뼈대에 색과 크기를 입히는 파일. `.cell.k-personal { background: 연한초록 }` 은
"연차로 찍힌 칸은 연한 초록으로 그려라"는 뜻.
→ 선택자, 반응형(`@media`), 다크 모드

### Q3. JavaScript 는?
**A.** 움직임을 적은 파일. HTML·CSS 는 가만히 있고, 누르면 반응하게 하는 것이 JS.
달력 그리기, 연차 계산, 데이터 받아오기가 전부 JS 다.

### Q4. 서버가 뭐야?
**A.** 요청을 받아 답을 돌려주는 **프로그램**. 그게 도는 컴퓨터도 서버라 부른다.
"서버를 빌렸다" = "24시간 켜져 있고 요청에 답하는 컴퓨터를 빌렸다".
특별한 기계가 아니다.

### Q5. 웹이 정확히 뭐야? 브라우저와 뭐가 달라?
**A.** 층이 다르다.
- **인터넷** — 컴퓨터를 잇는 망 (도로)
- **웹** — 그 망 위에서 HTTP 로 문서를 주고받는 **서비스 하나** (택배 회사)
- **브라우저** — 웹을 이용하는 **프로그램** (택배를 받아 여는 사람)

**웹 ≠ 인터넷.** 이메일도 인터넷을 쓰지만 웹이 아니다.
→ 이 구분이 Q13(메일) 이해의 열쇠

### Q6. 브라우저는 정확히 뭘 해?
**A.** 여섯 가지를 한다.
1. 주소를 받아 DNS 에 물어 IP 를 찾는다
2. HTTP 요청을 보낸다
3. 받은 HTML 을 해석해 화면을 그린다 (렌더링)
4. CSS 를 적용한다
5. JavaScript 를 실행한다 (엔진이 내장돼 있다)
6. **보안 규칙을 강제한다** — 다른 출처로의 요청 차단, IMAP 같은 다른 프로토콜 금지

6번이 핵심. 브라우저는 편의 도구이자 **감시자**다.

---

## 2부 · 주소와 연결

### Q7. HTTP 가 뭐야?
**A.** 클라이언트와 서버가 대화하는 규칙(프로토콜). 요청 하나에 응답 하나.
HTTPS 는 그 대화를 암호화한 것.

### Q8. 요청에 메서드와 상태 코드가 붙는 거야?
**A.** **아니다. 서로 다른 쪽에 붙는다.**
- 요청(request) → **메서드** (`GET` `PUT` `POST` `OPTIONS`)
- 응답(response) → **상태 코드** (`200` `401` `404` `500`)

메서드는 "뭘 하려는지"(가는 쪽), 상태 코드는 "어떻게 됐는지"(오는 쪽).

### Q9. 주소가 정확히 뭐야? 어디에 존재해?
**A.** 세 가지가 얽혀 있다.
- **IP 주소** — 서버의 진짜 번호. `104.21.45.178`. 인터넷은 이 숫자로만 찾아간다
- **도메인** — 사람이 읽는 별명. `workers.dev`. 그 자체로는 아무 힘이 없다
- **DNS** — "도메인 → IP" 기록을 보관하는 전 세계 분산 등록부

**주소는 DNS 서버들에 기록으로 존재한다.** 파일이 아니라 등록부다.

### Q10. 도메인은 아무렇게나 지을 수 있어? 돈 주고 산다는 건 뭐야?
**A.** 전 세계에서 유일해야 해서 **선착순 등록제**다. `naver.com` 을 못 쓰는 이유.
연 1~2만원짜리 **연 단위 임대**이고, 갱신을 안 하면 풀린다.

우리 주소가 공짜인 이유는 **남의 도메인 아래 방을 하나 받은 것**이기 때문:

```
salary-api . william040408 . workers . dev
    호수           동          건물    구획
   내가 지음    가입 때 받음  Cloudflare 소유
```

건물 안에서만 안 겹치면 되므로 `salary-api` 는 자유롭게 정할 수 있었다.

---

## 3부 · GitHub

### Q11. GitHub 이 뭐야? 왜 여러 개처럼 보여?
**A.** 한 회사, 한 계정 아래 여러 서비스가 있다. 우리는 셋을 쓴다.

| 별명 | 정식 명칭 | 하는 일 |
| --- | --- | --- |
| 창고 | Repository | 코드 보관, 변경 이력 |
| 자판기 | Pages | `docs` 폴더를 웹으로 |
| 일꾼 | Actions | 시키면 깨어나 실행 |

**셋은 서로 자동으로 엮이지 않는다.** 이게 헷갈림의 근원.

### Q12. Pages 는 진짜 `docs` 폴더만 보내? push 하면 누구한테 보내?
**A.** 그렇다. 다만 **실시간으로 창고를 읽는 게 아니다.**
push → GitHub 이 감지 → `docs` 를 Pages 서버로 **복사(배포)** → 그때부터 그 복사본으로 답한다.
그래서 1분쯤 걸린다.

"누구한테" — **주소를 아는 아무에게나.** 자판기는 누가 요청했는지 따지지 않는다.
그래서 급여를 여기 둘 수 없다.

### Q13. Actions 는 YAML 로 워크플로를 쓰고, 언제 깨어나 뭘 할지 적는 거지?
**A.** 맞다. 덧붙이면 **러너**(실행용 가상 컴퓨터)가 **매번 빈 상태로 새로 만들어지고**
일이 끝나면 통째로 사라진다. 그래서 파이썬도 매번 다시 깐다.

→ 이 성질 때문에 3~5월 명세서가 지워지는 사고가 났다 (Q22)

### Q14. GitHub 에도 저장 기능이 있는데 왜 KV 를 썼어?
**A.** 세 가지가 걸린다.
1. 저장소가 공개라 급여가 드러난다
2. **브라우저가 저장소에 쓸 수 없다** — 쓰려면 토큰이 필요하고, 페이지에 토큰을 두면 공개된다.
   비공개 저장소로 바꿔도 이 문제는 그대로다
3. Git 은 "코드 변경 이력" 도구다. 달력을 누를 때마다 커밋이 쌓이면 이력이 쓰레기가 된다

---

## 4부 · Cloudflare

### Q15. Workers 는 서버야? 나와 가까운 곳에 있어?
**A.** 서버가 맞다. 코드(`worker.js`)는 **전 세계 300여 도시에 전부 복사**되어 있고,
요청자와 가장 가까운 곳에서 실행된다. 이것을 **엣지**라 한다.
코드가 4KB 라 전부 복제해도 부담이 없다.

### Q16. KV 도 거기 다 저장돼 있어? 유튜브가 한국에 영상을 캐시해두는 것처럼?
**A.** **그 비유가 정확하다.** 다만 Workers 코드와는 방식이 다르다.

| | Workers 코드 | KV 데이터 |
| --- | --- | --- |
| 방식 | 전부 복제 | 중앙 원본 + 지역 캐시 |
| 쓰기 | 배포할 때만 | 중앙으로 가고 전 세계에 최대 60초 |
| 읽기 | 즉시 | 가까운 캐시부터 |

쓰고 바로 읽으면 옛 값이 나올 수 있다. 이를 **최종 일관성**이라 한다.
은행 잔고 같은 데는 쓰면 안 되지만 우리 용도에는 충분하다.

---

## 5부 · 메일

### Q17. 메일이 뭐야? HTTP 가 없던 시절에 서버가 어떻게 있었어?
**A.** **인터넷과 웹은 다르다.** 인터넷(망)이 먼저 있었고, 그 위에서 도는 서비스로
메일이 먼저, 웹이 나중에 생겼다.

```
1969  인터넷(ARPANET)  — 컴퓨터를 잇는 망
1971  이메일           — 그 망 위의 첫 서비스 중 하나
1983  DNS              — 도메인 체계
1991  웹(HTTP)         — 그 망 위의 또 다른 서비스
```

서버는 HTTP 와 무관한 개념이다. 요청을 받고 답하면 서버다.
메일 서버는 메일 프로토콜로 답하는 서버였고, 사람들은 **전용 프로그램**
(터미널 명령, 나중에 Outlook 같은 메일 클라이언트)으로 접속했다.

### Q18. 메일 주소의 `@` 뒤는 뭐야?
**A.** 그 사람의 편지를 보관하는 **메일 서버의 도메인**.
`william040408@gmail.com` 이면 `gmail.com` 의 메일 서버에 편지가 쌓인다.
도메인이므로 DNS 로 찾아가는 것도 웹과 똑같다.

### Q19. SMTP 가 뭐야?
**A.** Simple Mail Transfer Protocol — **편지를 보내고 서버끼리 전달하는** 규칙.

```
보내는 프로그램 ──SMTP──▶ 내 메일 서버 ──SMTP──▶ 상대 메일 서버
                                                      │
                                            받는 사람 ◀┘ IMAP 으로 꺼냄
```

우체국으로 치면 SMTP 는 부치고 배달하는 과정, IMAP 은 우편함에서 꺼내는 과정이다.

### Q20. IMAP 도 웹에서 서버로 보내는 프로토콜이야?
**A.** **웹이 아니다.** 프로그램에서 메일 서버로 직접 가는 별개의 문이다.

```
HTTP  ──▶  웹사이트를 보는 문
IMAP  ──▶  우편함을 여는 문
```

Gmail 웹사이트(`mail.google.com`)는 HTTP 로 접속하는 **껍데기**이고,
구글이 뒤에서 대신 메일 서버를 뒤져 화면에 보여주는 것이다.
우리 프로그램은 그 껍데기를 안 거치고 `imap.gmail.com` 에 직접 접속한다.

### Q21. 그럼 브라우저는 왜 IMAP 을 못 해? Python 으로 우회하는 거야?
**A.** "우회"가 아니라 **제한이 없는 환경에서 실행**하는 것이다.

JavaScript 라는 언어가 IMAP 을 못 하는 게 아니다. **브라우저가 그 기능을 안 준다.**
아무 웹사이트나 방문자의 메일을 읽을 수 있으면 큰일이니 일부러 막아 둔 것이다.
같은 JavaScript 도 Node.js(브라우저 밖)에서는 IMAP 을 할 수 있다.

우리는 Python 을 썼을 뿐, 언어가 중요한 게 아니라 **브라우저 밖이라는 점**이 중요하다.

---

## 6부 · 인증과 연결

### Q22. 단축 URL 이 뭐야?
**A.** 긴 주소를 대신 안내해 주는 중계소.

```
① 브라우저: "l.ecount.com/4664Q85K 주세요"
② 중계 서버: "301 — 진짜 주소는 여기입니다"
③ 브라우저: 그 주소로 다시 요청
```

중계 서버는 내용을 갖고 있지 않다. 주소만 알려준다.
쓰는 이유: 메일에 긴 주소를 안 넣으려고, 클릭 수를 세려고, 나중에 목적지를 바꾸려고.

### Q23. 세션이 뭐야?
**A.** HTTP 는 **요청이 끝나면 아무것도 기억하지 않는다**(무상태).
그래서 요청마다 "누구세요?"를 다시 물어야 한다.

해결책이 **임시 번호표**다.
1. 한 번만 신원 확인
2. 서버가 번호표(세션 ID)를 발급
3. 브라우저가 쿠키에 담아 매 요청에 제시
4. 서버는 "이 번호표 = 아까 그 사람"으로 기억

병원 접수 번호표, 콘서트 손목밴드와 같다. 시간이 지나면 만료된다.

### Q24. 이카운트는 왜 로그인 없이 보이고, 왜 번호표가 필요해?
**A.** 두 단계로 나눠 보면 된다.

**왜 로그인이 없나** — 이카운트는 회사가 쓰는 시스템이고 나는 계정이 없다.
그래서 회사가 명세서를 보낼 때 **일회용 비밀 링크**를 만들어 메일로 보낸다.
그 링크를 아는 사람 = 메일을 받은 사람 이라고 보고 통과시킨다.
**링크 자체가 신분증**이다.

**왜 번호표가 필요한가** — 명세서 화면 하나를 그리는 데도 요청이 여러 번 오간다
(화면, 이미지, PDF 변환…). 매번 그 긴 비밀 링크를 들고 다니면 번거롭고 위험하다.
그래서 처음 한 번 링크를 확인한 뒤 세션을 발급하고, 그 뒤로는 번호표로 다닌다.

**3개월 만료**도 같은 개념이다. 신분증을 영원히 유효하게 두면 위험하니까.

### Q25. 토큰과 앱 비밀번호는 뭐가 달라?
**A.** 둘 다 "프로그램이 쓰는 비밀번호"라는 점은 같다.
- **앱 비밀번호** — Google 이 IMAP 접속용으로 발급. 권한을 좁히지 못한다(Gmail 전체)
- **토큰** — 권한을 좁힐 수 있다. 우리 `GH_TOKEN` 은 "이 저장소의 Actions 만"

더 좁은 권한이 필요하면 **OAuth**(비밀번호를 주지 않고 특정 권한만 위임)를 쓰지만
설정이 훨씬 복잡하다.

---

## 7부 · 우리 시스템의 설계

### Q26. 왜 Cloudflare Workers 가 GitHub Actions 를 깨워?
**A.** 토큰을 숨기려고.

폰에서 직접 GitHub 을 깨우려면 `GH_TOKEN` 이 페이지 안에 있어야 하는데,
페이지는 자판기가 **아무에게나** 내준다. 누구나 소스를 열어 토큰을 꺼낼 수 있다.

문지기가 대신 들고 있으면 토큰은 서버 안에만 남는다.
이런 구조를 **프록시(중계)** 라 한다.

### Q27. git push 하면 정확히 뭐가 움직여?
**A.** 창고가 갱신되고, **두 곳이 각자 가져간다.**

| 무엇이 | 무엇을 보고 | 언제 |
| --- | --- | --- |
| 자판기 (Pages) | `docs/index.html` | 약 1분 |
| 문지기 (Workers) | `worker/worker.js`, `wrangler.jsonc` | 약 2분 |
| 일꾼 (Actions) | — 움직이지 않음 | — |
| 금고 (KV) | — 건드리지 않음 | — |

둘은 서로 기다리지도, 통신하지도 않는다.

### Q28. 그럼 GitHub 과 Cloudflare 는 데이터를 주고받는 거야?
**A.** **웹사이트가 열릴 때는 아니다.** 폰이 양쪽에 따로 물어본다.

둘이 직접 이야기하는 경우는 두 번뿐이고, 둘 다 Cloudflare → GitHub 방향이며
급여 데이터와 무관하다.
- 코드를 올릴 때 — Cloudflare 가 창고에서 문지기 코드를 가져감
- 버튼을 누를 때 — 문지기가 GitHub API 로 일꾼을 깨움

### Q29. Node.js 는 뭐고 왜 안 깔았어?
**A.** JavaScript 를 브라우저 밖에서 실행하는 프로그램. Python 과 같은 자리에 있다.
Cloudflare 배포 도구(wrangler)가 이것으로 만들어져 있지만
**Cloudflare 쪽 컴퓨터에서 돌아가므로** 내 노트북에는 필요 없다.

---

## 8부 · 언어와 실행 환경

### Q30. JavaScript 는 JSON 같은 거야? C·Python 같은 언어는 아니지 않아?
**A.** **JavaScript 는 C·Python 과 같은 부류의 프로그래밍 언어다.** JSON 과는 전혀 다르다.

| | 정체 | 실행되나 |
| --- | --- | --- |
| JSON | 자료를 적는 **형식** | 안 됨. 그냥 글자 |
| JavaScript | 프로그래밍 **언어** | 됨 |

이름이 비슷한 건 JSON 이 "JavaScript Object Notation" 의 줄임말이라서다.
JavaScript 의 문법에서 표기법만 빌려온 것이지 같은 것이 아니다.
JSON 은 명사(자료), JavaScript 는 동사(동작)에 가깝다.

### Q31. Node.js 와 그냥 JavaScript 는 뭐가 달라?
**A.** **같은 언어, 다른 실행 환경.** 언어는 혼자 돌지 못하고 실행 환경이 있어야 한다.

| 할 수 있는 것 | 브라우저 | Node.js | Cloudflare Workers |
| --- | --- | --- | --- |
| 화면 조작 (`document`) | O | X | X |
| 파일 읽기·쓰기 | X | O | X |
| TCP 연결 (IMAP 등) | X | O | 제한적 |
| 서버 열기 | X | O | O(다른 방식) |
| 다른 주소로 요청 | 허락 필요 | 자유 | 자유 |

같은 사람이 학교(브라우저)와 집(Node)에서 할 수 있는 일이 다른 것과 같다.
**Cloudflare Workers 는 세 번째 환경**이며 Node.js 가 아니다.

### Q32. Python 으로 IMAP 을 어떻게 보내?
**A.** IMAP 은 결국 **글자로 된 명령을 주고받는 것**이다. Python 표준 도구 `imaplib` 가
그 명령을 대신 만들어 준다.

```
프로그램 → 서버:  a1 LOGIN 주소 앱비밀번호
서버 → 프로그램:  a1 OK LOGIN completed
프로그램 → 서버:  a2 SELECT INBOX
서버 → 프로그램:  a2 OK [READ-WRITE] SELECT completed
```

특별한 마법이 아니라 연결을 열고 약속된 문장을 보내는 것이다.
Python 이 특별해서가 아니라 **브라우저 밖이라 운영체제가 허락해 주기 때문**이다.

### Q33. 브라우저가 "보안 규칙을 강제한다"는 게 무슨 뜻이야?
**A.** 웹사이트의 코드는 **모르는 사람이 보낸 프로그램**이다. 그것이 내 컴퓨터를
마음대로 하면 안 되므로 브라우저가 격리된 방에 가두고 실행한다.

브라우저가 막는 것:
- 내 컴퓨터의 파일 읽기
- 다른 사이트로 마음대로 요청 보내기 (CORS 없이는)
- 다른 탭의 내용 엿보기
- IMAP 같은 다른 프로토콜 쓰기
- 카메라·위치는 물어보고 허락받아야 함

막지 않으면 생기는 일: 악성 사이트를 열었을 뿐인데 그 사이트의 코드가
내 은행 사이트에 몰래 송금 요청을 보낼 수 있다.

---

## 9부 · 프로토콜 구분

### Q34. HTTP·SMTP·IMAP 을 다시 구분해 줘. 누가 누구에게 보내는 거야?
**A.** 대화 상대와 목적이 다르다.

| | 누가 | 누구에게 | 무엇을 |
| --- | --- | --- | --- |
| HTTP | 브라우저·프로그램 | **웹 서버** | 파일·데이터를 달라 |
| SMTP | 메일 프로그램, 메일 서버 | **메일 서버** | 편지를 보낸다(밀어넣기) |
| IMAP | 메일 프로그램 | **메일 서버** | 편지를 꺼낸다(가져오기) |

한 컴퓨터가 여러 서버를 동시에 돌릴 수 있고, **포트 번호**로 구분한다.
- HTTP 80 / HTTPS 443
- SMTP 25, 587 / IMAP 143, 993

같은 건물에 문이 여러 개 있고 문마다 담당이 다른 것과 같다.

### Q35. 인터넷은 물리적인 망이야?
**A.** 물리적인 선 + **그 선으로 데이터를 주고받는 약속(TCP/IP)** 까지가 인터넷이다.
선만 있으면 전선이지 인터넷이 아니다.

### Q36. 그럼 웹은 정확히 무슨 서비스야?
**A.** **하이퍼링크로 서로 연결된 문서들의 묶음**, 그리고 그것을 HTTP 로 주고받는 체계.
"웹(거미줄)"이라는 이름은 문서끼리 링크로 얽혀 있어서 붙었다.
지금은 문서뿐 아니라 앱도 웹 위에서 돌지만, 바탕은 여전히 문서를 나르는 구조다.

---

## 10부 · 흐름 확인

### Q37. 이카운트의 번호표는 실제로 어떻게 작동해?
**A.**
1. 서버가 **무작위로 긴 문자열**을 만든다 — 이것이 세션 ID
2. 서버는 자기 쪽에 "이 문자열 = 조원준의 8월 명세서" 를 기록해 둔다
3. 브라우저에 그 문자열을 건넨다 (주소에 붙이거나 쿠키로)
4. 다음 요청마다 브라우저가 그 문자열을 같이 보낸다
5. 서버가 자기 기록을 뒤져 누구인지 알아낸다

우리가 본 주소의 `SESSION_ID=$HXQSS…:AB-Z6006…` 가 바로 그 문자열이다.
**서버가 기억하고 있어야 의미가 있다.** 문자열 자체에는 정보가 없다.

### Q38. 웹사이트를 여는 과정을 정리하면?
**A.** 실제 순서는 이렇다. (Actions 는 여기 등장하지 않는다)

1. 크롬에 주소 입력
2. DNS 조회 → `william040408.github.io` 의 IP — **GitHub Pages 서버**
3. HTTP GET → 숫자가 없는 HTML 을 받음
4. 브라우저가 HTML·CSS 를 그리고 JavaScript 를 실행
5. JS 가 이 기기에 저장해 둔 암구호를 꺼냄
6. JS 가 `salary-api.…workers.dev` 로 요청 (여기서 DNS 조회가 한 번 더)
7. **Cloudflare Workers** 가 암구호를 확인하고 KV 에서 꺼내 JSON 으로 응답
8. JS 가 화면의 빈칸을 채움

흔한 오해:
- HTML 은 Cloudflare 가 아니라 **GitHub Pages** 에 있다
- Cloudflare Workers 는 **Node.js 가 아니다** (별도 실행 환경)
- **Actions 는 버튼을 눌렀을 때만** 깨어난다. 페이지를 여는 것과 무관하다

---

## 아직 안 물어본 것 (문서로 만들 때 채울 만한 것)

- 쿠키와 브라우저 저장소(localStorage)의 차이
- HTTPS 의 암호화가 실제로 무엇을 가리는가
- CORS 가 막는 공격이 구체적으로 무엇인가
- 왜 비밀번호를 서버가 그대로 저장하면 안 되는가
- Git 의 브랜치와 병합
- 왜 명세서를 파싱할 때 정규식을 썼는가
